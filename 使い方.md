# 推しログ (OshiLog) - 取扱説明書

推し活の「お金」「当落戦績」「スケジュール」「欲しいものリスト」「リンク集」をブラウザ上で一元管理できる、ローカル完結型のシングルページアプリケーション（SPA）です。

URL：https://stellular-parfait-11a86c.netlify.app/
にアクセスするだけで、プライバシーを守った状態ですぐ利用できます。

---

## 🎯 概要・コンセプト
チケットの当落結果から遠征費の細かな内訳、グッズの欲しいものリストまで、推し活に伴うあらゆる情報を整理するために開発されました。
すべてのデータはお使いのブラウザ（`localStorage`）にのみ保存されるため、プライバシーが完全に守られ、サーバーやデータベースの構築も一切不要です。`index.html` ファイルをブラウザで開くだけですぐに使い始めることができます。

---

## 🚀 使い方
1. 本リポジトリから `index.html` ファイルをダウンロードするか、公開されているURL（GitHub Pages等）にアクセスします。
2. ローカルで使う場合は、PCやスマートフォンのブラウザ（Chrome, Safari, Edgeなど）で `index.html` を開きます。
3. 初回起動時にサンプルデータが作成されます。右下の「設定」タブから推しの情報を編集して利用を開始してください。

---

## 🧭 全機能・画面仕様解説

### 1. 全画面共通（ヘッダー ＆ ナビゲーション）
アプリの最上部と最下部に常駐し、データの切り替えと画面遷移を制御するシステム領域です。
* **ヘッダー領域**
  * **ロゴ ＆ タイトル:** アプリ名「推しログ (OshiLog)」を表示。
  * **推し切り替えドロップダウン:** 登録されている「推しマスタ」の一覧を読み込み、選択された推しのデータへ画面全体を一瞬で切り替えます。
  * **「推しを追加」ボタン:** ポップアップが開き、新しい推しの名前を入力することで、完全に独立した2人目・3人目のマスタ（箱）を作成します。
* **ボトムナビゲーション領域**
  * 画面の書き換え（SPA構造）を行う全8種類のタブボタン。選択中のタブはテーマカラーにハイライトされます。
  * 【ホーム / 記録（入力） / 活動（一覧） / 当落 / 統計 / 欲しい / リンク / 設定】

### 2. 「ホーム」画面
現在の推しのステータスと、直近の活動、今月の動向を瞬時に俯瞰するためのダッシュボードです。
* **推しプロフィールカード**
  * **アイコン表示:** 設定された絵文字、またはアップロードされて正方形にクロップ・圧縮された推しの顔写真を表示。
  * **推し名 ＆ 所属:** 登録された名前とグループ名を表示。
  * **推し歴（カウンター）:** 「推し始めた日」から本日までの経過日数を自動計算し、「〇日目」としてリアルタイム表示。
  * **誕生日バッジ:** 推しの誕生日（月日のみ）を表示。
* **クイック統計パネル（4つのインジケータ）**
  * **総活動回数:** 「予定（未確定）」を除いた、過去の実績回数の合計。
  * **総支出:** 過去の実績データに紐づく4分割金額の総総合計。
  * **今月の活動 / 今月の支出:** 当月（システム日付の月）に実施された実績回数および支出合計。
* **最近の推し活（直近3件リスト）**
  * 未来の「予定」も含め、日付が新しい順に最大3件のミニカードを表示。
  * カードをクリックすると「活動一覧」タブへ移動し、対象の「詳細モーダル」を自動展開します。

### 3. 「記録」画面（新規登録）
新しい活動履歴、または未来のスケジュール（予定）を手動で書き込むためのメインフォームです。
* **基本入力:** 日付（デフォルトは本日）、タイトル・内容入力（必須）。
* **活動の種類:** 以下から1つを選択。
  * 【ライブ / 握手会等 / グッズ購入 / 配信・動画視聴 / 映画鑑賞 / 舞台鑑賞 / 聖地訪問 / ショップ訪問 / ファンアート作成 / その他】
* **「これは予定です」チェックボックス**
  * **ONの場合:** 支出内訳フォームと満足度の入力を自動ロック（グレーアウト）し、金額を強制的に「0円」として保存。カレンダー上のスケジュール予約として機能します。
  * **OFFの場合:** フォームが活性化し、実績データ（4分割金額・満足度星評価）の入力が可能。
* **支出の内訳フォーム（4分割）:** 「チケット代等」「ホテル代」「交通費」「その他」に分けて1円単位で入力。
* **満足度（星評価）:** 1〜5個の星をタップして直感的にレーティング。
* **関連リンク追加機能:** その活動専用の「URL」と「サイト名」を紐付け可能（複数可）。

### 4. 「活動」画面（一覧・検索）
これまでに記録したすべての「予定」と「実績」を時系列（降順）で管理・検索する画面です。
* **検索・フィルタ:** キーワードによるリアルタイムタイトル検索、および種別フィルタドロップダウン。
* **活動リスト表示**
  * **左端のボーダー色:** 「実績」はピンク色、「予定」はグレーの細線で視覚的に区別。
  * **各種バッジ:** 未来の予定データには「予定」バッジ、リンク付きデータには直接クリック可能な「リンク」バッジを表示。
* **詳細確認 ＆ 削除:** 行をクリックすると詳細・編集モーダルが起動。右端のゴミ箱ボタンで即座に削除可能。

### 5. 「当落」画面（当落管理ダッシュボード）
チケット抽選の戦績分析と、現場スケジュールの生成を行う特化型の画面です。
* **戦績スコアボード（ダッシュボード）**
  * 期間（年）、種類、場所（登録データから重複排除して自動生成）でフィルタ可能。
  * 「結果待ち」を除外した確定数から「〇戦〇勝（勝率〇％）」を巨大な文字でリアルタイム計算・表示。
* **当落カードリスト**
  * ステータスバッジ（結果待ち：黄 / 当選：ピンク / 落選：グレー）付きで公演日順に表示。
  * **「コピー」ボタン:** イベント名・場所・種類を完全コピーしつつ、ステータスを「結果待ち」に初期化して新規モーダルを展開（連続入力用）。
  * **「活動へ」ボタン:** 「当選」かつ「未連携」のデータのみ出現。押すと「記録」タブへジャンプし、内容を引き継いだ状態で「予定」登録画面を自動展開。
* **当落登録・編集モーダル**
  * 場所コンボボックスは過去の入力から自動補完。一番下の「＋ 新しい場所を追加」から新しい会場名を直接タイピングして追加可能。

### 6. 「統計」画面
蓄積されたお金と活動のデータをグラフで視覚化し、客観的に分析する画面です。
* **4大メトリクス:** 「総実績回数」「総支出額」「平均満足度」「推し活日数」を表示（予定データは除外）。
* **月別支出推移（棒グラフ）:** 横軸に「月」、縦軸に「その月の総支出額」をとったバーチャート。
* **活動種別内訳（円グラフ）:** 単なる回数ではなく「使った金額の総額」をベースにしたドーナツチャート。どこに一番お金を投資しているかが一目でわかります。

### 7. 「欲しい」画面（ウィッシュリスト）
買いたいグッズや円盤をリスト化し、購入時に支出管理へ連動させる買い物サポート画面です。
* **リスト管理:** 商品名、参考URL、予想価格、優先度（高：赤 / 中：黄 / 低：グレー）を入力・表示。
* **購入連動機能:** 詳細モーダル内の「購入済みにする」ボタンを押すと、「グッズ購入」という種類で、商品名と予想価格が「その他支出」に自動セットされた「活動記録（予定）」を自動生成して活動一覧へデータを転送。リスト上は「購入済（グレーアウト）」に切り替わります。

### 8. 「リンク」画面（リンク集）
FC公式、チケプラ、公式グッズストアなど、日常的にアクセスする外部サイトをブックマークする場所です。
* **リンクカード:** タイトル、URL、自分用のメモ（会員番号など）を記録。タイトルをクリックすると、別タブ（`target="_blank"`）で対象サイトが即座に開きます。

### 9. 「設定」画面 ＆ データユーティリティ
アプリの心臓部である「推しマスタ」の書き換えと、ローカル環境特有のデータ管理を行うエリアです。
* **プロフィール編集 ＆ 自動画像軽量化:** 写真を選択すると、Canvas要素を展開して**自動で中央を検知して正方形にトリミング**し、解像度を `256x256` にリサイズ。容量節約のため、透過情報を維持できる高効率な **`WebP` 形式（品質80%）に超圧縮**して `localStorage` に保存します。
* **エクスポート (`exportData()`):** 全データを1つのきれいな `JSONファイル` にまとめ、PCやスマホにダウンロード（バックアップ）。
* **インポート (`importData()`):** バックアップしたJSONファイルを読み込み。データの構造が正しいかを自動チェックし、現在のブラウザ環境を一括上書きして完全に元の状態へ復元。

---

## ⚠️ 今後の課題・検討事項 (Issues)

本アプリは手軽さと安全性を優先し `localStorage`（容量上限: 約5MB）を採用していますが、長期間の運用において以下の課題とジレンマを抱えており、将来的なアップデートの検討材料としています。

### 1. データ容量の肥大化とシステム移行（IndexedDB）の壁
画像の自動圧縮によりテキストのみであれば数年〜十数年レベルで運用可能ですが、最終的な容量限界に達した際、`IndexedDB` へのシステム移行が選択肢に挙がります。しかし、IndexedDBは「OSやブラウザの都合（端末のストレージ不足など）で、ユーザーの許可なく勝手にデータが消去される（Eviction）」という致命的なリスクがあり、安全なローカル管理というコンセプトと相反する懸念があります。

### 2. アーカイブ運用における「生涯戦績」の喪失感
容量限界の回避策として、「年次や区切りのタイミングでJSONをエクスポートし、アプリ側のデータをリセットする（アーカイブ運用）」が最も現実的です。
しかし、この運用を行うと、本アプリの醍醐味であるダッシュボードの「累計総支出額」や「生涯の当落勝率」がゼロにリセットされてしまうため、推し活のモチベーションや「歴史の積み重ね」を視覚的に楽しめなくなるデメリットが生じます。

### 3. マスタデータと履歴データの分離
アーカイブ運用を前提とした場合、現状の「全データクリア機能」では、推しのプロフィールやリンク集、未購入のウィッシュリストといった【マスタデータ】まで消去されてしまいます。
今後の改修として、「活動履歴と当落データ（履歴データ）のみを消去し、基本設定は引き継ぐ（部分クリア機能）」の実装が求められます。
